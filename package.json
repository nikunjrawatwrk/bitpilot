{
  "name": "bitbucket-pr-reviewer",
  "version": "1.0.0",
  "description": "Bitbucket Pull Request Reviewer - Web Application",
  "scripts": {
    "dev": "fuser -k 3001/tcp 2>/dev/null; concurrently -n server,client -c blue,green \"npm run server\" \"npm run client\"",
    "server": "node server/index.js",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "NODE_ENV=production node server/index.js",
    "install:all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
